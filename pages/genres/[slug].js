import Layout from "../../components/layout"
const Genres = ({ globalpage, navigation }) => {
    return (
        <Layout globalpage={globalpage} navigation={navigation} >
            <div>genres</div>
        </Layout>
    )
}
export default Genres