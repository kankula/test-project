import Layout from '../components/layout'
const About = ({ globalpage, navigation }) => {
    return (
        <Layout globalpage={globalpage} navigation={navigation} >
            <div>about</div>
        </Layout>
    )
}
export default About